desc Test --test is a table;

set serveroutput on;
declare
    v_name test.name%type;
begin
select name into v_name from test
where id=1;
dbms_output.put_line(v_name);
end;
/

declare
    v_name varchar2(8);
begin
select name into v_name from test
where id=1;
dbms_output.put_line(v_name);
end;

alter table test modify name char(10);

commit;